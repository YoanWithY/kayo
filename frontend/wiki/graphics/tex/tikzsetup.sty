\ProvidesPackage{tikzsetup}
\usepackage{graphicx, tikz, xcolor, calc}
\usepackage{fontspec}
\usetikzlibrary {arrows.meta}
\usetikzlibrary{angles}
\usepackage{tikz-3dplot}

\newcommand{\slopednode}[5]{
    % Calculate the angle between the two points
    \pgfmathanglebetweenpoints{\pgfpointanchor{#1}{center}}{\pgfpointanchor{#2}{center}}
    \edef\angleAB{\pgfmathresult}

    % Create the node with the specified text and rotation angle
    \node[#3, rotate=\angleAB] (#4) at ($(#1)!0.5!(#2)$) {#5};
}
\tikzset{>={Stealth[inset=0, angle=30:4pt]}} % globaly set latex arrow type
\definecolor{myGray}{RGB}{80,80,80}
\tikzset{
    vertex/.style={circle, minimum size=4pt, draw=gray, fill=myGray, fill opacity = 1, inner sep=0, outer sep=0pt, text width=0}
}
\tikzset{
    vertnode/.style={circle, minimum size=3pt, fill=gray, fill opacity = 1, inner sep=0, outer sep=0pt, text width=0}
}
\tikzset{
    facenode/.style={circle, minimum size=4pt, draw=myGray,inner sep=0, outer sep=0pt, text width=0}
}
\tikzset{
    edgenode/.style={rectangle, minimum height=4pt, minimum width=8pt, draw=gray, fill=myGray, fill opacity = 1, inner sep=0, outer sep=0pt, text width=0}
}
\tikzset{
vector/.style={->={-Stealth[inset=0, angle=30:4pt]}, thick, opacity = 1}
}
\tikzset{
    concaveAngle/.style={draw=red, fill=red, fill opacity=0.5, angle radius=5pt}
}
\tikzset{
    convexAngle/.style={draw=green, fill=green, fill opacity=0.5, angle radius=15pt}
}
\tikzset{
    directedMeshGraph/.style={
            color=myGray,
        }
}
\pgfdeclarelayer{z1}
\pgfsetlayers{main,z1}
\setmainfont{Arial}